import styled from 'styled-components';

const FollowerList = styled.div`
  padding: 15px;

  .no-data {
    text-align: center;

    h2 {
      padding: 40px 0;

      font-size: 20px;
      font-weight: 600;
    }
  }
`;

export default FollowerList;
